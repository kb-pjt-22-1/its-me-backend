<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.benepay.domain.merchant.mapper.MerchantBrandMapper">

    <select id="findAll" resultType="site.benepay.domain.merchant.vo.MerchantBrand">
        SELECT brand_id, brand_code, brand_name, brand_logo
        FROM merchant_brands
        ORDER BY brand_id
    </select>

</mapper>
