<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.benepay.domain.bookmark.mapper.BookmarkMapper">

    <sql id="bookmarkColumns">
        bookmark_id, user_id, merchant_id, created_at
    </sql>

    <insert id="upsertBookmark">
        INSERT INTO bookmarked_stores (user_id, merchant_id)
        VALUES (#{userId}, #{merchantId})
        ON DUPLICATE KEY UPDATE is_deleted = FALSE
    </insert>

    <update id="softDeleteBookmark">
        UPDATE bookmarked_stores
        SET is_deleted = TRUE
        WHERE user_id = #{userId}
          AND merchant_id = #{merchantId}
          AND is_deleted = FALSE
    </update>

    <select id="findActiveByUserId" resultType="site.benepay.domain.bookmark.vo.Bookmark">
        SELECT <include refid="bookmarkColumns" />
        FROM bookmarked_stores
        WHERE user_id = #{userId}
          AND is_deleted = FALSE
        ORDER BY created_at DESC
    </select>

    <select id="findByUserIdAndMerchantId" resultType="site.benepay.domain.bookmark.vo.Bookmark">
        SELECT <include refid="bookmarkColumns" />
        FROM bookmarked_stores
        WHERE user_id = #{userId}
          AND merchant_id = #{merchantId}
          AND is_deleted = FALSE
    </select>

</mapper>
